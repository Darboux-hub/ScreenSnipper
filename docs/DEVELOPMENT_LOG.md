# Development Log

本文件记录 `ScreenSnipper` 的工程开发过程，按日期倒序维护。

---

## 2026-04-22

### 背景

- 需要将当前本地迭代成果整理为可持续维护的桌面截图工具项目。

### 改动

- 建立 GitHub 友好的文档与协作基线。
- 规划并补齐 `AGENT.md`、`README.md`、`LICENSE`、`.gitignore`。
- 明确源码仓库只提交源码、资源与文档，不提交本地构建环境与产物。

### 验证

- 对当前目录结构进行盘点，确认已有源码、资源、spec 与构建产物。
- 确认当前目录尚未初始化 git，因此文档中补充了仓库初始化建议。

### 遗留 / 待办

- 后续可补充 `requirements.txt` 或自动化发布流程。

---

## 2026-04-22

### 背景

- 需要将悬浮窗 UI 从自绘按钮升级为更完整的图片化入口，并让托盘视觉保持一致。

### 改动

- 接入 PNG 悬浮窗资源。
- 让悬浮窗与托盘图标使用同源图片资源。
- 新增三档悬浮窗大小，并支持持久化。
- 将悬浮窗默认位置升级为主显示器居中，首次升级时强制居中一次。
- 统一所有截图入口在抓屏前先隐藏悬浮窗。

### 验证

- 验证图片资源可正常加载并带透明通道。
- 验证三档大小缩放结果可生成。
- 验证首次新版布局初始化时会居中。
- 验证截图启动前悬浮窗已隐藏，失败后会恢复。
- 重新打包 `ScreenSnipper.exe` 并确认资源已纳入构建。

### 遗留 / 待办

- 后续可以继续微调悬浮窗视觉比例与托盘小尺寸显示效果。

---

## 2026-04-22

### 背景

- 需要为常驻工具增加更直接的入口与更灵活的控制方式。

### 改动

- 新增悬浮按钮入口。
- 新增热键设置对话框与持久化。
- 引入 `QSettings` 存储位置、热键与相关设置。
- 托盘与悬浮按钮统一共用菜单逻辑。

### 验证

- 验证悬浮按钮可显示、隐藏、拖动和恢复位置。
- 验证热键设置可被保存，并在热键冲突时回滚。
- 验证程序在热键不可注册时仍可通过悬浮按钮和托盘工作。

### 遗留 / 待办

- 中文字符串历史上出现过乱码，需要后续持续关注编码问题。

---

## 2026-04-22

### 背景

- 需要让截图工具支持后台常驻和随系统启动。

### 改动

- 增加系统托盘常驻逻辑。
- 增加当前用户登录自启动支持。
- 使用 `PyInstaller` 打包无控制台单文件 EXE。
- 将自启动入口切换为打包后的 EXE 路径。

### 验证

- 验证托盘初始化与清理链路。
- 验证注册表 `Run` 项可读写。
- 验证 `PyInstaller` 单文件打包成功。

### 遗留 / 待办

- 后续可以考虑 GitHub Release 分发策略。

---

## 2026-04-22

### 背景

- 需要做一个类似微信/QQ 的轻量级截图工具。

### 改动

- 完成基于 `PyQt5 + Win32 API` 的单文件截图主程序。
- 实现全局热键、全屏冻结、遮罩层框选、剪贴板写入和取消逻辑。
- 支持多显示器虚拟桌面截图。

### 验证

- 验证脚本可通过 `py_compile`。
- 验证初始化链路、热键注册和清理流程。
- 验证截图窗口类、拖拽选区和取消逻辑可正常工作。

### 遗留 / 待办

- 后续继续提升悬浮 UI、托盘体验与文档整理质量。

